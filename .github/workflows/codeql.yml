name: CodeQL

# Static security analysis (SAST) across all three languages in this repo.
# Findings show up under the repo's Security tab -> Code scanning alerts.
# Free on public repos, no separate license needed.
on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # weekly, Monday 06:00 UTC
  workflow_dispatch: {}

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
          - language: go
            build-mode: autobuild
          - language: java-kotlin
            build-mode: manual
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-java@v4
        if: matrix.language == 'java-kotlin'
        with:
          distribution: temurin
          java-version: '8'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Autobuild
        if: matrix.build-mode == 'autobuild'
        uses: github/codeql-action/autobuild@v3

      # The java services are git submodules, each with its own pom.xml one
      # level down (services/<name>/pom.xml) -- autobuild only looks at the
      # repo root and can't find them, so build each one explicitly instead.
      - name: Build Java services
        if: matrix.language == 'java-kotlin'
        run: |
          for s in carts orders shipping queue-master; do
            echo "building $s..."
            mvn -q -B -f services/$s/pom.xml compile -DskipTests
          done

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
